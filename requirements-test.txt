pytest>=8.0
# async test support — the gateway retry and re-index lock tests are coroutines.
pytest-asyncio>=0.23
httpx>=0.27
# backend/generator.py loads .env at import time, so the unit tests that import
# it need this even though no real key is used.
python-dotenv>=1.0
# LLM-judged regression suite (scripts/deepeval_eval.py); judge = OpenAI,
# needs OPENAI_API_KEY (or run with --no-llm for keyword checks only).
deepeval>=4.0
